# deploy/worker-01/.env
# Compose 文件插值变量（供 docker compose YAML 中 ${VAR} 使用）
# 复制本文件为 .env，填写真实值。.env 已被 .gitignore 排除。

# Redis 认证密码（compose.platform.yml 中 redis command 插值使用）
PLATFORM_REDIS_PASSWORD=change-me-to-a-strong-password
