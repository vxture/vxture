# deploy/worker-01/.env
# compose.platform.yml 中 redis service 命令插值用（${REDIS_PASSWORD}）
# 由 setup-env.sh 自动生成，禁止提交到 git
#
# 注意：此文件仅供 Docker Compose YAML 变量替换使用
#       服务容器内的 REDIS_URL 来自 secrets/platform.env

REDIS_PASSWORD=CHANGE_ME
