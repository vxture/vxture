# deploy/worker-02/.env
# Compose 文件插值变量（供 docker compose YAML 中 ${VAR} 使用）
# 复制本文件为 .env，填写真实值。.env 已被 .gitignore 排除。

# Vela Redis 密码
VELA_PROD_REDIS_PASSWORD=change-me-to-a-strong-password
VELA_BETA_REDIS_PASSWORD=change-me-to-a-strong-password

# Ruyin Redis 密码
RUYIN_PROD_REDIS_PASSWORD=change-me-to-a-strong-password
RUYIN_BETA_REDIS_PASSWORD=change-me-to-a-strong-password
