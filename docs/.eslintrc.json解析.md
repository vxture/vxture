# .eslintrc.json 配置文件详解

本文档详细解释了 Vxture 项目中 `.eslintrc.json` 文件的各个部分及其作用，帮助开发者理解 ESLint 代码规范检查配置。

## 什么是 .eslintrc.json？

`.eslintrc.json` 是 ESLint 的配置文件，用于定义代码质量和编码风格规则。ESLint 是一个静态代码分析工具，用于识别和报告 JavaScript/TypeScript 代码中的问题模式，确保代码质量和一致性。

## 配置文件概览

```json
{
  "root": true,
  "parser": "@typescript-eslint/parser",
  "parserOptions": { ... },
  "extends": [ ... ],
  "plugins": [ ... ],
  "env": { ... },
  "rules": { ... },
  "settings": { ... },
  "overrides": [ ... ]
}
```

## 配置选项详解

### 基础配置

```json
{
  "root": true,
  "parser": "@typescript-eslint/parser",
  "parserOptions": {
    "ecmaVersion": 2022,
    "sourceType": "module",
    "ecmaFeatures": {
      "jsx": true
    },
    "project": "./tsconfig.json"
  }
}
```

| 选项               | 值                            | 说明                                                                |
| ------------------ | ----------------------------- | ------------------------------------------------------------------- |
| `root`             | `true`                        | 指定该配置文件为项目的根配置文件，ESLint 不会继续向上级目录查找配置 |
| `parser`           | `"@typescript-eslint/parser"` | 指定使用 TypeScript ESLint 解析器，允许 ESLint 解析 TypeScript 代码 |
| `parserOptions`    | 对象                          | 解析器的配置选项                                                    |
| `ecmaVersion`      | `2022`                        | 指定支持的 ECMAScript 版本                                          |
| `sourceType`       | `"module"`                    | 指定代码为 ECMAScript 模块                                          |
| `ecmaFeatures.jsx` | `true`                        | 启用 JSX 支持                                                       |
| `project`          | `"./tsconfig.json"`           | 指定 TypeScript 配置文件路径，用于类型检查                          |

### 扩展配置

```json
{
  "extends": [
    "next/core-web-vitals",
    "plugin:react/recommended",
    "plugin:jsx-a11y/recommended",
    "plugin:import/errors",
    "plugin:import/warnings",
    "plugin:import/typescript",
    "plugin:@typescript-eslint/recommended",
    "plugin:security-node/recommended",
    "prettier"
  ]
}
```

`extends` 字段允许继承其他配置预设，按照顺序应用（后面的会覆盖前面的冲突规则）：

| 扩展配置                                  | 说明                                                   |
| ----------------------------------------- | ------------------------------------------------------ |
| `"next/core-web-vitals"`                  | Next.js 推荐的配置，包括优化 Web Vitals 性能指标的规则 |
| `"plugin:react/recommended"`              | React 推荐规则集，确保 React 最佳实践                  |
| `"plugin:jsx-a11y/recommended"`           | JSX 无障碍访问推荐规则，确保网站可访问性               |
| `"plugin:import/errors"`                  | 检测导入语句错误                                       |
| `"plugin:import/warnings"`                | 检测导入语句的潜在问题                                 |
| `"plugin:import/typescript"`              | 针对 TypeScript 导入的支持                             |
| `"plugin:@typescript-eslint/recommended"` | TypeScript 推荐规则集                                  |
| `"plugin:security-node/recommended"`      | Node.js 安全性规则集                                   |
| `"prettier"`                              | 与 Prettier 配合使用，禁用可能与 Prettier 冲突的规则   |

### 插件配置

```json
{
  "plugins": [
    "react",
    "jsx-a11y",
    "import",
    "@typescript-eslint",
    "sonarjs",
    "react-perf",
    "prettier",
    "unused-imports",
    "security-node"
  ]
}
```

`plugins` 字段列出了项目使用的 ESLint 插件，每个插件提供了一组规则：

| 插件                   | 说明                                      |
| ---------------------- | ----------------------------------------- |
| `"react"`              | React 特定的规则，检查常见问题和最佳实践  |
| `"jsx-a11y"`           | 无障碍访问规则，确保 JSX 元素的可访问性   |
| `"import"`             | 检查导入/导出语句，防止路径错误和重复导入 |
| `"@typescript-eslint"` | TypeScript 特定的规则，提供类型检查       |
| `"sonarjs"`            | 提供代码质量和代码"异味"检测规则          |
| `"react-perf"`         | React 性能优化规则，避免常见的性能陷阱    |
| `"prettier"`           | 将 Prettier 格式化规则集成到 ESLint 中    |
| `"unused-imports"`     | 检测和自动删除未使用的导入                |
| `"security-node"`      | Node.js 安全性检查，防止安全漏洞          |

### 环境配置

```json
{
  "env": {
    "browser": true,
    "node": true,
    "es2022": true
  }
}
```

`env` 字段指定代码运行的环境，预定义了特定环境中可用的全局变量：

| 环境              | 说明                                             |
| ----------------- | ------------------------------------------------ |
| `"browser": true` | 启用浏览器全局变量（如 `window`, `document` 等） |
| `"node": true`    | 启用 Node.js 全局变量和作用域                    |
| `"es2022": true`  | 启用 ES2022 语法特性和全局变量                   |

### 规则配置

`.eslintrc.json` 的 `rules` 部分非常重要，它定义了具体的代码规范检查规则。为了更好理解，我们按类别分组介绍：

#### React 相关规则

```json
{
  "rules": {
    "react/react-in-jsx-scope": "off",
    "react/prop-types": "off",
    "react-hooks/rules-of-hooks": "error",
    "react-hooks/exhaustive-deps": "warn",
    "react/jsx-key": "error",
    "react/no-unescaped-entities": "warn",
    "react/display-name": "off"
  }
}
```

| 规则                            | 值        | 说明                                                   |
| ------------------------------- | --------- | ------------------------------------------------------ |
| `"react/react-in-jsx-scope"`    | `"off"`   | 不再需要在每个文件中导入 React（React 17+ 不需要）     |
| `"react/prop-types"`            | `"off"`   | 关闭 prop-types 检查，因为项目使用 TypeScript 类型     |
| `"react-hooks/rules-of-hooks"`  | `"error"` | 强制 React Hooks 规则，确保 Hooks 只在函数组件顶层调用 |
| `"react-hooks/exhaustive-deps"` | `"warn"`  | 检查 useEffect/useCallback 的依赖项列表，防止遗漏      |
| `"react/jsx-key"`               | `"error"` | 要求列表渲染时提供 key 属性                            |
| `"react/no-unescaped-entities"` | `"warn"`  | 警告 JSX 中未转义的特殊字符                            |
| `"react/display-name"`          | `"off"`   | 关闭组件显示名称检查                                   |

#### 无障碍访问规则

```json
{
  "rules": {
    "jsx-a11y/anchor-is-valid": "warn",
    "jsx-a11y/alt-text": "error",
    "jsx-a11y/img-redundant-alt": "error",
    "jsx-a11y/aria-role": "error"
  }
}
```

| 规则                           | 值        | 说明                                            |
| ------------------------------ | --------- | ----------------------------------------------- |
| `"jsx-a11y/anchor-is-valid"`   | `"warn"`  | 警告无效的 `<a>` 标签用法                       |
| `"jsx-a11y/alt-text"`          | `"error"` | 要求图片元素有 alt 属性                         |
| `"jsx-a11y/img-redundant-alt"` | `"error"` | 防止 alt 文本中包含多余的"image"、"picture"等词 |
| `"jsx-a11y/aria-role"`         | `"error"` | 要求 ARIA 角色属性值有效                        |

#### TypeScript 相关规则

```json
{
  "rules": {
    "@typescript-eslint/no-unused-vars": "off",
    "@typescript-eslint/no-explicit-any": "warn",
    "@typescript-eslint/explicit-function-return-type": "off",
    "@typescript-eslint/explicit-module-boundary-types": "off",
    "@typescript-eslint/no-non-null-assertion": "warn"
  }
}
```

| 规则                                                  | 值       | 说明                                             |
| ----------------------------------------------------- | -------- | ------------------------------------------------ |
| `"@typescript-eslint/no-unused-vars"`                 | `"off"`  | 关闭原始未使用变量检查，使用 unused-imports 替代 |
| `"@typescript-eslint/no-explicit-any"`                | `"warn"` | 警告使用 `any` 类型                              |
| `"@typescript-eslint/explicit-function-return-type"`  | `"off"`  | 不强制要求函数显式返回类型                       |
| `"@typescript-eslint/explicit-module-boundary-types"` | `"off"`  | 不强制要求导出函数和类的显式返回类型             |
| `"@typescript-eslint/no-non-null-assertion"`          | `"warn"` | 警告使用非空断言操作符 `!`                       |

#### 导入相关规则

```json
{
  "rules": {
    "import/order": [
      "warn",
      {
        "groups": [
          "builtin",
          "external",
          "internal",
          ["parent", "sibling"],
          "index",
          "object",
          "type"
        ],
        "newlines-between": "always",
        "alphabetize": {
          "order": "asc",
          "caseInsensitive": true
        }
      }
    ],
    "import/no-duplicates": "error",
    "import/no-unresolved": "off"
  }
}
```

| 规则                     | 值         | 说明                                                                                     |
| ------------------------ | ---------- | ---------------------------------------------------------------------------------------- |
| `"import/order"`         | 复杂配置   | 强制导入排序规则，按特定顺序分组并添加空行                                               |
| `"groups"`               | 数组       | 导入分组顺序：内置模块 → 外部依赖 → 内部模块 → 相对导入 → 索引文件 → 对象导入 → 类型导入 |
| `"newlines-between"`     | `"always"` | 不同组导入之间添加空行                                                                   |
| `"alphabetize"`          | 对象       | 按字母顺序排序导入，不区分大小写                                                         |
| `"import/no-duplicates"` | `"error"`  | 禁止重复导入同一模块                                                                     |
| `"import/no-unresolved"` | `"off"`    | 关闭未解析模块路径检查，TypeScript 已处理                                                |

#### 代码质量规则

```json
{
  "rules": {
    "no-console": ["warn", { "allow": ["warn", "error", "info"] }],
    "no-debugger": "warn",

    "sonarjs/cognitive-complexity": ["warn", 15],
    "sonarjs/no-duplicate-string": ["warn", { "threshold": 3 }],
    "sonarjs/no-identical-functions": "warn",

    "security-node/detect-insecure-randomness": "warn",

    "react-perf/jsx-no-new-object-as-prop": "warn",
    "react-perf/jsx-no-new-array-as-prop": "warn",
    "react-perf/jsx-no-new-function-as-prop": "warn"
  }
}
```

| 规则                                         | 值                             | 说明                                     |
| -------------------------------------------- | ------------------------------ | ---------------------------------------- |
| `"no-console"`                               | `["warn", { "allow": [...] }]` | 警告使用 console，但允许 warn/error/info |
| `"no-debugger"`                              | `"warn"`                       | 警告使用 debugger 语句                   |
| `"sonarjs/cognitive-complexity"`             | `["warn", 15]`                 | 限制函数认知复杂度不超过 15              |
| `"sonarjs/no-duplicate-string"`              | `["warn", { "threshold": 3 }]` | 警告重复字符串出现超过 3 次              |
| `"sonarjs/no-identical-functions"`           | `"warn"`                       | 警告完全相同的函数实现                   |
| `"security-node/detect-insecure-randomness"` | `"warn"`                       | 警告使用不安全的随机数生成               |
| `"react-perf/jsx-no-new-object-as-prop"`     | `"warn"`                       | 警告在 JSX 中内联创建对象（影响性能）    |
| `"react-perf/jsx-no-new-array-as-prop"`      | `"warn"`                       | 警告在 JSX 中内联创建数组（影响性能）    |
| `"react-perf/jsx-no-new-function-as-prop"`   | `"warn"`                       | 警告在 JSX 中内联创建函数（影响性能）    |

#### 未使用导入检查

```json
{
  "rules": {
    "unused-imports/no-unused-imports": "warn",
    "unused-imports/no-unused-vars": [
      "warn",
      {
        "vars": "all",
        "varsIgnorePattern": "^_",
        "args": "after-used",
        "argsIgnorePattern": "^_",
        "ignoreRestSiblings": true,
        "caughtErrors": "all",
        "caughtErrorsIgnorePattern": "^_"
      }
    ]
  }
}
```

| 规则                                 | 值       | 说明                                   |
| ------------------------------------ | -------- | -------------------------------------- |
| `"unused-imports/no-unused-imports"` | `"warn"` | 警告未使用的导入语句                   |
| `"unused-imports/no-unused-vars"`    | 复杂配置 | 警告未使用的变量                       |
| `"vars": "all"`                      |          | 检查所有变量                           |
| `"varsIgnorePattern": "^_"`          |          | 忽略以下划线开头的变量（如 `_unused`） |
| `"args": "after-used"`               |          | 只检查使用位置之后的参数               |
| `"argsIgnorePattern": "^_"`          |          | 忽略以下划线开头的参数                 |
| `"ignoreRestSiblings": true`         |          | 忽略解构赋值中的剩余部分               |
| `"caughtErrors": "all"`              |          | 检查所有 catch 子句错误参数            |
| `"caughtErrorsIgnorePattern": "^_"`  |          | 忽略以下划线开头的错误参数             |

### 设置配置

```json
{
  "settings": {
    "react": {
      "version": "detect"
    },
    "import/resolver": {
      "typescript": {
        "project": "./tsconfig.json"
      },
      "node": {
        "extensions": [".js", ".jsx", ".ts", ".tsx"]
      }
    },
    "import/extensions": [".js", ".jsx", ".ts", ".tsx"]
  }
}
```

`settings` 字段提供共享设置：

| 设置                  | 值         | 说明                                      |
| --------------------- | ---------- | ----------------------------------------- |
| `"react.version"`     | `"detect"` | 自动检测项目中使用的 React 版本           |
| `"import/resolver"`   | 对象       | 配置导入解析器                            |
| `"typescript"`        | 对象       | TypeScript 解析器配置，指向 tsconfig.json |
| `"node"`              | 对象       | Node.js 解析器配置，指定支持的文件扩展名  |
| `"import/extensions"` | 数组       | 指定导入时支持的文件扩展名                |

### 特定文件覆盖配置

```json
{
  "overrides": [
    {
      "files": ["*.tsx", "*.ts"],
      "rules": {
        "@typescript-eslint/explicit-function-return-type": "off"
      }
    },
    {
      "files": ["*.js", "*.jsx"],
      "parser": "espree",
      "parserOptions": {
        "ecmaVersion": 2022,
        "sourceType": "module",
        "ecmaFeatures": {
          "jsx": true
        }
      }
    },
    {
      "files": ["next.config.js", "tailwind.config.js", "postcss.config.js"],
      "rules": {
        "@typescript-eslint/no-var-requires": "off"
      }
    },
    {
      "files": [
        "**/contexts/**/*.tsx",
        "**/hooks/**/*.ts",
        "**/components/**/*.tsx",
        "**/app/**/*.tsx"
      ],
      "rules": {
        "react-perf/jsx-no-new-object-as-prop": "off",
        "react-perf/jsx-no-new-function-as-prop": "off",
        "react-perf/jsx-no-new-array-as-prop": "off"
      }
    },
    {
      "files": ["**/utils/**/*.ts"],
      "rules": {
        "security-node/detect-insecure-randomness": "off"
      }
    }
  ]
}
```

`overrides` 字段允许为特定文件指定不同的规则：

| 覆盖配置 | 目标文件          | 说明                                                                       |
| -------- | ----------------- | -------------------------------------------------------------------------- |
| 第1项    | `"*.tsx", "*.ts"` | 对所有 TypeScript 文件关闭显式函数返回类型要求                             |
| 第2项    | `"*.js", "*.jsx"` | 对 JavaScript 文件使用 espree 解析器                                       |
| 第3项    | 配置文件          | 对配置文件允许 CommonJS 的 require 语法                                    |
| 第4项    | 组件/Hooks/上下文 | 对组件、钩子和上下文文件关闭 React 性能警告（这些地方常需要内联函数/对象） |
| 第5项    | 工具函数          | 对工具函数关闭不安全随机数警告                                             |

## 最佳实践

1. **规则严重程度级别**：
   - `"off"` 或 `0`：完全禁用规则
   - `"warn"` 或 `1`：将规则视为警告（不影响退出码）
   - `"error"` 或 `2`：将规则视为错误（触发时退出码为1）

2. **VSCode 集成**：
   - 安装 ESLint 扩展以在编辑器中实时显示问题
   - 配置保存时自动修复：
     ```json
     // .vscode/settings.json
     {
       "editor.codeActionsOnSave": {
         "source.fixAll.eslint": true
       }
     }
     ```

3. **自定义规则**：
   - 根据项目需求调整规则严重程度
   - 添加或删除规则以适应开发团队习惯

4. **忽略特定文件**：
   - 使用 `.eslintignore` 文件或文件内的注释：`// eslint-disable-line`

5. **解决规则冲突**：
   - 多个扩展可能有冲突规则，通常应将 `"prettier"` 放在 `extends` 列表最后
   - 显式设置冲突规则的值以覆盖预设

6. **处理 TypeScript 和 React**：
   - 确保 `tsconfig.json` 路径正确
   - 使用 React 特定规则确保组件符合最佳实践
