{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "description": "FastAPI backend for vxture platform",
  "scripts": {
    "dev": "python start_dev.py",
    "dev:direct": ".venv/Scripts/python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python start_server.py",
    "test": "pytest",
    "lint": "ruff check .",
    "format": "ruff format ."
  },
  "engines": {
    "python": ">=3.11"
  }
}
