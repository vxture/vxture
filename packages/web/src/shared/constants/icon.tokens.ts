/**
 * Icon Tokens (Semantic Only)
 *
 * 设计原则：
 * 1. Icon 仅表达「业务语义」，不表达样式、情绪或装饰
 * 2. 内容层只能使用此处定义的 token
 * 3. UI 层负责 token → 具体 icon（SVG / Font / React Component）
 * 4. 禁止在内容 JSON 中新增或临时定义 icon
 *
 * 扩展规则（非常重要）：
 * - 只有当「多个模块/页面都会复用」时，才允许新增 token
 * - 单一页面、单一文案需求 → ❌ 不新增
 * - 装饰性、营销情绪类 icon（如 fire / star / heart）→ ❌ 不新增
 */

export const ICON_TOKENS = [
  /* =========================
   * 数据 / 分析
   * ========================= */
  'data', // 泛指数据能力（比 database 更抽象）
  'chart', // 统计 / 指标 / 可视化
  'graph', // 关系 / 链路 / 网络

  /* =========================
   * AI / 智能
   * ========================= */
  'ai', // 通用 AI 能力
  'brain', // 推理 / 决策
  'spark', // 智能触发 / 洞察

  /* =========================
   * 系统 / 平台
   * ========================= */
  'cloud', // 云 / 平台 / 托管
  'code', // 技术 / 开发 / API
  'cog', // 系统配置 / 能力引擎
  'layers', // 架构 / 模块化

  /* =========================
   * 安全 / 治理
   * ========================= */
  'shield', // 安全 / 防护
  'lock', // 权限 / 隐私
  'check', // 合规 / 校验 / 可靠

  /* =========================
   * 方案 / 业务能力
   * ========================= */
  'users', // 用户 / 协作 / 组织
  'calendar', // 计划 / 调度 / 生命周期
  'refresh', // 同步 / 更新
  'bot', // Agent / 自动化

  /* =========================
   * UI / 通用交互
   * ========================= */
  'lightning', // 快速 / 即时（可接受的唯一“感受型”icon）
  'globe', // 国际化 / 多语言

  /* =========================
   * 联系方式 / 外部
   * ========================= */
  'mail',
  'phone',
  'github',
  'wechat',
] as const;

export type IconToken = (typeof ICON_TOKENS)[number];
